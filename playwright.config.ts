import { defineConfig, devices } from "@playwright/test";

/**
 * End-to-end smoke tests. These require a running server (npm run dev or a
 * production build) plus valid LLM credentials, so they are kept in a separate
 * test:e2e script and run in their own CI job rather than the fast unit gate.
 */

export default defineConfig({
  testDir: "./tests/e2e",
  timeout: 60_000,
  expect: { timeout: 10_000 },
  fullyParallel: false,
  retries: 0,
  reporter: [["list"]],
  use: {
    baseURL: process.env.BASE_URL || "http://localhost:3000",
    trace: "on-first-retry",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
  webServer: {
    command: "npm run start",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
